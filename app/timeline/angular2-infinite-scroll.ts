import { InfiniteScroll } from '../timeline/infinite-scroll';
import { Scroller } from '../timeline/scroller';

export * from '../timeline/infinite-scroll';
export * from '../timeline/scroller';

export default {
	directives: [ InfiniteScroll, Scroller ]
}